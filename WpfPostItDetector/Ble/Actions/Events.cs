using SDKSmartTrainnerAdaptor.Ble.Configuration;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace SDKSmartTrainnerAdaptor.Ble.Actions
{
    public partial class BLEMethods
    {  
  
        public void addEvents()
        {
/*
            adapter.DeviceConnected += (o, args) =>
            {
                //args.Device.UpdateConnectionInterval(ConnectionInterval.Low);

                rootClass.loggerAdd("Connected: " + args.Device.Name);
               
            };


            adapter.DeviceDisconnected += (o, args) =>
            {
                rootClass.loggerAdd("Disconected: " + args.Device.Name);
                };

            adapter.DeviceConnectionLost += (o, args) =>
            { 
            };

            adapter.DeviceConnectionError += (o, args) =>
            {
                rootClass.loggerAdd("Connection Error: " + args.Device.Name);
            };


            adapter.DeviceDiscovered += (o, args) =>
            {
                rootClass.loggerAdd("Found: " + args.Device.Name);
                Task.Delay(100);

                
            };


            adapter.ScanTimeoutElapsed += (o, args) =>
            {
                rootClass.loggerAdd("Scan finish, found: "+WorkingDataBLE.Current.SessonData.DevicesDetected.Count());
   
            };


            adapter.DeviceConnectionError += (o, args) =>
            {
                rootClass.loggerAdd("Connection error to device: " + args.Device.Name);
            };
 
            */
        }


 
 
    }
}
